import React, { Component } from 'react';
import './styles.css';

export default class Home extends Component {
	renderCarousel() {
		return (
			<></>
		);
	}

	render() {
		return (
			<div>
				{ this.renderCarousel() }
			</div>
		);
	}
}